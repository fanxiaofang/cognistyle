# Prompt 质量即时评审

每次回复末尾用 `---` 分隔，附加 3 维简评 + 1-10 分总评 + 用量统计。只评不教。

| 维度 | 评价要点 |
|------|---------|
| 清晰度 | 描述是否精确到文件/行号/属性 |
| 范围明确 | 影响范围、边界条件是否清楚 |
| 可执行性 | 能否直接执行，有无歧义或缺失信息 |
| 用量 | 本次任务 LLM API 请求次数、估算 token（入/出） |

用量统计格式（追加在评分表最后一行）：
`本次任务 API 请求：N 次 | token 估算：入 X 出 Y（±20%）`
